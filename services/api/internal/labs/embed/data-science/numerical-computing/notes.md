## NumPy in TraceLab

- **Production** serves this lesson from **embedded lab files** under `labs/data-science/concepts/numerical-computing/` (synced into the Go API at build time).
- **Local dev**: run `docker compose up datascience` and hit `/api/datascience/api/playground/numpy-demo` via the Go proxy to execute real NumPy.
