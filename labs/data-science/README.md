# Data science lab (authoring)

Lesson copy and simulator parameters for this lab live in `services/api/internal/labs/data/data-science.json` today (inline Python/markdown).

Use **`sandbox/`** here for local notebooks or scripts while prototyping; promoting content into the shipped JSON (or a future `present.py` embed) is described in **`../CONCEPT.md`**.
