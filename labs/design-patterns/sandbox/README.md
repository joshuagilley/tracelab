Scratch space for experiments while you design a concept: throwaway `main` packages, tests, alternate APIs, and notes. Nothing here is served by the API.

When you are happy with the code, copy or merge the relevant parts into the concept’s `present.go` (see `labs/CONCEPT.md`).
