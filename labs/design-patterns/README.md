# Design patterns lab (authoring)

- **Sandbox:** `sandbox/` — local experiments, not shipped.
- **Published code for the UI:** `concepts/<slug>/present.go` — synced into the Go API embed tree and referenced from `services/api/internal/labs/data/design-patterns.json`.

See **`../CONCEPT.md`** for the full workflow.
